# 🦉 Discord Бот

Полнофункциональный Discord бот с модульной архитектурой для администрирования сервера, управления семьями, розыгрышами и мониторинга статистики.

## 📋 Функции

### 🤖 Возможности бота
- **Временные голосовые каналы**: Автоматическое создание войсов с панелью управления (слоты, приватность, переименование, передача владельца)
- **Система заявок в семью**: Создание заявок с интерактивными кнопками и модальными окнами
- **Розыгрыши (Giveaways)**: Система конкурсов с кнопкой участия, требованиями к ролям и автоопределением победителей
- **Логирование**: Отслеживание входа/выхода пользователей с сервера
- **Администрирование**: Команды для модерации и управления сервером
- **Экономика**: Система баланса и уровней с прогрессией XP
- **Профили пользователей**: XP, репутация, статистика сообщений, время в голосе

### 🛡️ Модерация
- Фильтр матерных слов (настраиваемый список для каждого сервера)
- Очистка сообщений (purge)

## 🚀 Установка

### Требования
- Python 3.11+
- MySQL/MariaDB
- Discord.py 2.3+

### 1. Установка зависимостей Ubuntu
```bash
sudo apt update && sudo apt upgrade
sudo apt install apache2 mariadb-server mariadb-client php libapache2-mod-php php-mysql php-redis php-zip phpmyadmin
sudo systemctl restart apache2
```

### 2. Настройка базы данных
```sql
-- Вход в MySQL
sudo mysql

-- Создание пользователя
CREATE USER 'serveradmin'@'%' IDENTIFIED BY 'ваш_пароль';
GRANT ALL PRIVILEGES ON *.* TO 'serveradmin'@'%';
FLUSH PRIVILEGES;

-- Настройка удалённого доступа (опционально)
# Отредактируйте /etc/mysql/mariadb.conf.d/50-server.cnf
# Закомментируйте строку: bind-address = 127.0.0.1
```

### 3. Установка Python-зависимостей
```bash
pip install -r requirements.txt
```

## 🔧 Конфигурация

### Файл `.env`
```env
# Discord Bot
DISCORD_TOKEN=ваш_токен_бота

# Database
DB_HOST=localhost
DB_USER=serveradmin
DB_PASSWORD=ваш_пароль
DB_NAME=discord_bot

# Bot Settings
LOG_LEVEL=INFO
BOT_PREFIX=!
```

## 🚀 Запуск

### Запуск бота
```bash
python main.py
```

## 📚 Команды бота

### 👤 Профиль и экономика (`commands/profile.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!профиль [@упоминание]` | `!проф`, `!profile` | Просмотр профиля пользователя (XP, уровень, баланс, время в голосе, сообщения) | Все |
| `!setbalance <@user> <сумма>` | — | Установка баланса пользователю | Owner (владелец бота) |

---

### 🎁 Розыгрыши (`commands/giveaway.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!giveaway` | `!gw` | Создать розыгрыш через интерактивное меню (приз, длительность, победители, требования) | Manage Guild |

---

### 🛡️ Администрирование (`commands/admin.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!clear <количество>` | `!purge`, `!очистить` | Очистка сообщений в канале (до 100) | Manage Messages |
| `!setwelcome <канал>` | — | Настройка канала для приветствий новых участников | Administrator |
| `!setgoodbye <канал>` | — | Настройка канала для прощаний участников | Administrator |
| `!sendmessage <#канал> <текст>` | `!sendmsg`, `!msg` | Отправка сообщения от имени бота в указанный канал | Administrator |

---

### 🚫 Фильтр мата (`commands/profanity_filter.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!add_banned_word <слово>` | — | Добавить слово в чёрный список сервера | Administrator |
| `!remove_banned_word <слово>` | — | Удалить слово из чёрного списка | Administrator |
| `!list_banned_words` | — | Показать все запрещённые слова на сервере | Administrator |
| `!toggle_profanity` | — | Включить/выключить фильтр матерных слов | Administrator |
| `!profanity_stats` | — | Показать статистику срабатываний фильтра | Administrator |

---

### 📜 Заявки на семью (`commands/family_applications.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!setup_family` | — | Настроить систему заявок (каналы, роль, аудит) | Administrator |
| `!family_status` | — | Показать статус системы заявок (принято/отклонено/ожидает) | Administrator |
| `!family_applications` | — | Показать все заявки пользователя или ожидающие рассмотрения | Administrator |

---

### 📋 Меню (`commands/menu.py`)
| Команда | Алиасы | Описание | Права |
|---------|--------|----------|-------|
| `!menu` | `!меню` | Показать интерактивное меню со всеми функциями бота | Все |

---

### 🔊 Голосовые каналы (`commands/tempvoice.py`)
| Команды | Описание | Права |
|---------|----------|-------|
| **Текстовых команд нет** | Управление через кнопки на временных голосовых каналах: создание, переименование, лимит слотов, приватность, передача владельца | Владелец канала |

---

### 📅 События (`commands/events.py`)
| Команды | Описание | Права |
|---------|----------|-------|
| **Текстовых команд нет** | Автоматические события: приветствие новых участников, прощание ушедших, логирование | Бот |

---

## 📊 Итого
- **Всего текстовых команд:** 15
- **Модулей (cogs):** 8
- **Команды с русскими алиасами:** 4 (`!профиль`, `!очистить`, `!меню`, админские)
- **Без текстовых команд:** 2 модуля (`tempvoice`, `events`) — работают через события и UI-кнопки

> 💡 **Примечание:** Все команды нечувствительны к регистру. Например: `!Profile`, `!PROFILE`, `!PrOfIlE` — работают одинаково.

---

## 🗄️ Структура базы данных

### Таблицы, используемые ботом

| Таблица | Описание | Где используется |
|---------|----------|------------------|
| `users` | Пользователи Discord (XP, уровень, баланс, статистика) | `profile.py`, `events.py`, `mysql_connector.py` |
| `server_settings` | Настройки серверов (каналы приветствий/прощаний, каналы для временных войсов) | `admin.py`, `events.py`, `tempvoice.py`, `mysql_connector.py` |
| `voice_channels` | Временные голосовые каналы (ID канала, владелец, название) | `tempvoice.py` |
| `family_applications` | Заявки в семьи (статус, пользователь, дата) | `family_applications.py` |
| `family_settings` | Настройки системы заявок (каналы, роль, аудит) | `family_applications.py` |
| `profanity_words` | Запрещённые слова для фильтрации мата (на сервер) | `profanity_filter.py`, `mysql_connector.py` |
| `server_profanity_settings` | Настройки фильтра мата для серверов | `profanity_filter.py`, `mysql_connector.py` |
| `giveaways` | Розыгрыши (приз, победители, участники, статус, длительность) | `giveaway.py`, `mysql_connector.py` |

*Примечание: Таблицы `families`, `family_members`, `temp_voice_channels`, `servers`, `user_profiles` не используются в текущей версии бота.*

## 🔒 Безопасность

- ✅ Токен хранится в переменных окружения (`.env`)
- ✅ `.env` исключён из Git (`.gitignore`)
- ✅ Параметризованные SQL-запросы (защита от инъекций)
- ✅ Проверка прав доступа в командах

### ⚠️ Важно!
1. **Смените токен бота**, если он был закоммичен в историю Git
2. Регулярно делайте **бэкапы базы данных**

## 🏗️ Архитектура проекта

```
/workspace
├── main.py                 # Главный файл бота (загрузка cogs, события on_ready)
├── commands/               # Модули команд (cogs)
│   ├── admin.py            # Админ-команды (clear, setwelcome, setgoodbye, sendmessage)
│   ├── events.py           # События (welcome, goodbye)
│   ├── family_applications.py # Заявки в семью (модальные окна, кнопки)
│   ├── giveaway.py         # Система розыгрышей (giveaways)
│   ├── profile.py          # Профили и уровни (XP, баланс, voice_time)
│   ├── profanity_filter.py # Фильтр мата (настраиваемый список слов)
│   └── tempvoice.py        # Временные каналы (панель управления)
├── database/               # Работа с БД
│   └── mysql_connector.py  # Подключение к MySQL, функции для всех таблиц
├── .env                    # Переменные окружения (не в Git!)
├── .gitignore              # Исключения для Git
├── bot_logs.txt            # Лог-файл бота
└── README.md               # Документация
```

## 🎨 Технологии

| Компонент | Технология |
|-----------|------------|
| Бот | Discord.py 2.3+ |
| База данных | MySQL/MariaDB |
| DB драйвер | mysql-connector-python |
| HTTP клиент | aiohttp |

## 📈 Рекомендации по развитию

1. **Redis** — кэширование и управление сессиями
2. **Планировщик задач** — отложенная отправка сообщений
3. **Unit-тесты** — тестирование критических функций

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота: `bot_logs.txt`
2. Убедитесь, что БД доступна
3. Проверьте права бота в Discord

## 📝 Лицензия

Проект создан для образовательных целей. Используйте ответственно.

---

**Готово!** 🎉 Бот полностью функционален.
